# Backend API URL — Vite dev server proxies all /api/* routes to this target.
# Production Cloudflare Pages value:
# VITE_API_URL=https://tcgtracker-api.onrender.com
# Use localhost only when actively developing backend code:
# VITE_API_URL=http://localhost:3001

# Card Scanner Backend URL (optional — local dev defaults to http://localhost:5001)
# Optional direct fallback only; production normally uses the Node API proxy.
# VITE_CARD_SCANNER_API_URL=https://card-scanner-backend-cp2s.onrender.com
# VITE_CARD_SCANNER_API_URL=http://localhost:5001

# Sentry Configuration (Optional)
# VITE_SENTRY_DSN=your_sentry_dsn_here
# VITE_SENTRY_ENVIRONMENT=production

# Feature Flags
# VITE_ENABLE_AUTH=true
# VITE_ENABLE_ANALYTICS=false

# Google Analytics (Optional)
# VITE_GA_TRACKING_ID=G-XXXXXXXXXX

# App Metadata
# VITE_APP_NAME=TCGTracker
# VITE_APP_VERSION=1.0.0
