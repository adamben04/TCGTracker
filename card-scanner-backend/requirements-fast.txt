# Fast-path-only runtime for the cloud (Hugging Face Space) image.
# Deliberately OMITTED:
#   - pokemon-card-recognizer  (1.7 GB reference pkl bundle + EasyOCR weights)
#   - ollama                   (LLM grader, demoted / not used on scan path)
# The DINOv2 + cosine fast matcher handles ~95% of scans. When it isn't
# confident, app.py returns "no card" instead of running OCR.
flask==3.0.0
flask-cors==4.0.0
pillow>=10.1.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
onnxruntime>=1.17.0
# onnx (model loader dependency) - not strictly required at runtime, but harmless
onnx>=1.15.0
# Production WSGI server — Flask's dev server is single-process and not robust.
gunicorn>=21.2.0
